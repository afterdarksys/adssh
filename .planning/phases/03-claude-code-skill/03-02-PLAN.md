---
phase: 03-claude-code-skill
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/adssh/SKILL.md
  - .mcp.json
autonomous: true
requirements: [SKILL-01, SKILL-03]
must_haves:
  truths:
    - "SKILL.md exists at .claude/skills/adssh/SKILL.md with correct frontmatter"
    - "Invoking /adssh triggers Claude to connect + orient with list_sessions"
    - "SKILL.md loads .claude/adssh-reference.md via @ reference"
    - ".mcp.json exists at project root with adssh MCP server registration"
  artifacts:
    - path: ".claude/skills/adssh/SKILL.md"
      provides: "Trigger-based Claude Code skill for adssh operation"
      contains: "name: adssh"
    - path: ".mcp.json"
      provides: "MCP server registration for Claude Code"
      contains: "mcpServers"
  key_links:
    - from: ".claude/skills/adssh/SKILL.md"
      to: ".claude/adssh-reference.md"
      via: "@ file reference in skill body"
      pattern: "@.claude/adssh-reference.md"
    - from: ".mcp.json"
      to: "cmd/adssh-mcp"
      via: "command field pointing to binary"
      pattern: "adssh-mcp"
---

<objective>
Create the trigger-based SKILL.md file and the .mcp.json MCP server registration.

Purpose: SKILL.md is the Claude Code skill that fires on `/adssh`, executing the connect + orient sequence (check MCP connection, list sessions, give situational briefing). The .mcp.json registers the adssh-mcp binary so Claude Code can connect to it.

Output: `.claude/skills/adssh/SKILL.md` + `.mcp.json`
</objective>

<execution_context>
@/Users/ryan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-claude-code-skill/03-RESEARCH.md
@.planning/phases/03-claude-code-skill/03-PATTERNS.md
@.planning/phases/03-claude-code-skill/03-CONTEXT.md

<interfaces>
<!-- Skill format reference from existing skill -->

From ~/.claude/skills/careful/SKILL.md (frontmatter pattern):
```yaml
---
name: careful
version: 0.1.0
description: |
  Safety guardrails for destructive commands...
triggers:
  - be careful
  - safety mode
allowed-tools:
  - Bash
  - Read
---
```

Key observations:
- name: plain slug (no slashes)
- description: block scalar (|) for multiline
- triggers: YAML list
- allowed-tools: OMIT entirely for adssh per D-02

From cmd/adssh-mcp/main.go (binary startup):
- Flags: --policy (string), --api-key (string)
- Env override: ADSSH_MCP_API_KEY, ADSSH_POLICY

MCP registration pattern (.mcp.json):
```json
{
  "mcpServers": {
    "adssh": {
      "command": "/absolute/path/to/adssh-mcp",
      "env": {
        "ADSSH_MCP_API_KEY": "${ADSSH_MCP_API_KEY}",
        "ADSSH_POLICY": "${HOME}/.adssh/policy.rego"
      }
    }
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .claude/skills/adssh/SKILL.md trigger-based skill</name>
  <files>.claude/skills/adssh/SKILL.md</files>
  <read_first>
    /Users/ryan/.claude/skills/careful/SKILL.md
    .planning/phases/03-claude-code-skill/03-PATTERNS.md
    .planning/phases/03-claude-code-skill/03-CONTEXT.md
  </read_first>
  <action>
Create directory `.claude/skills/adssh/` and write `SKILL.md` with this exact content:

**Frontmatter (per D-02):**
```yaml
---
name: adssh
description: |
  Operate adssh sessions via MCP: session management, Starlark scripting,
  cloud queries, container execution, and audit log review. Invoke with /adssh.
triggers:
  - /adssh
  - adssh
---
```

Do NOT include `allowed-tools` key (per D-02 — no tool restriction).
Do NOT include `hooks` key (no pre-tool-use hooks needed).
Do NOT include `version` key (not required).

**Skill body (per D-03 — connect + orient mode):**

After the frontmatter `---` delimiter, write the skill body in Markdown:

```markdown
# /adssh -- adssh Session Operator

adssh MCP is now **active**. Execute the connect + orient sequence:

1. Confirm the `adssh` MCP server is available in your tools list
2. Call `list_sessions` to enumerate active SSH sessions
3. Report a situational briefing:
   - Number of active sessions and their IDs (or "no active sessions")
   - Available adssh tools: eval_starlark, run_shell, list_sessions, cloud_query, container_exec, audit_log
   - Ready status: "adssh operator ready"

If `list_sessions` returns an MCP error, the server is not connected.
Tell the user to verify:
- `.mcp.json` is present in the project root with the adssh server entry
- `adssh-mcp` binary is built (`go build -o adssh-mcp ./cmd/adssh-mcp`)
- Required env vars are set (at minimum `ADSSH_POLICY` pointing to a .rego file)

## Quick Reference

**Tool selection:**
- Cloud operations with arguments: `eval_starlark`
- Simple zero-arg cloud function: `cloud_query`
- Shell one-liners, file ops, grep/awk: `run_shell`
- Ephemeral container commands: `container_exec`
- Review activity or debug denials: `audit_log`

**Policy awareness:** All tool calls are gated by Rego policy. "access denied" means a policy rule blocked it. Use `audit_log` with `filter="denied"` to investigate.

**Audit trail:** Every tool call is logged. Use `audit_log` to review your history.

@.claude/adssh-reference.md
```

The `@.claude/adssh-reference.md` line at the end loads the companion reference document into context when the trigger fires.
  </action>
  <verify>
    <automated>test -f .claude/skills/adssh/SKILL.md && echo "EXISTS" || echo "MISSING"</automated>
    <automated>head -10 .claude/skills/adssh/SKILL.md</automated>
    <automated>grep -c "allowed-tools" .claude/skills/adssh/SKILL.md</automated>
    <automated>grep -c "@.claude/adssh-reference.md" .claude/skills/adssh/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/adssh/SKILL.md exists
    - File starts with `---` (YAML frontmatter delimiter)
    - Frontmatter contains `name: adssh`
    - Frontmatter contains `triggers:` with `/adssh` and `adssh` entries
    - Frontmatter does NOT contain `allowed-tools` (grep -c "allowed-tools" returns 0)
    - Body contains `# /adssh` heading
    - Body contains `list_sessions` (connect + orient step)
    - Body contains `adssh operator ready` or similar readiness status text
    - Body contains `@.claude/adssh-reference.md` (companion doc reference)
    - Body contains `filter="denied"` (policy awareness per D-10)
    - Body mentions all 6 tool names: eval_starlark, run_shell, list_sessions, cloud_query, container_exec, audit_log
  </acceptance_criteria>
  <done>
    .claude/skills/adssh/SKILL.md exists with correct frontmatter (name: adssh, triggers: [/adssh, adssh], no allowed-tools per D-02), connect + orient behavior (D-03), quick reference with tool selection (D-09) and policy awareness (D-10), and loads companion reference via @.claude/adssh-reference.md (D-01).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create .mcp.json MCP server registration</name>
  <files>.mcp.json</files>
  <read_first>
    cmd/adssh-mcp/main.go
    .planning/phases/03-claude-code-skill/03-CONTEXT.md
    .planning/phases/03-claude-code-skill/03-PATTERNS.md
  </read_first>
  <action>
Create `.mcp.json` at the project root with the env-var configuration form (per D-05 — env vars preferred, secrets not in args array).

The file must contain:
```json
{
  "mcpServers": {
    "adssh": {
      "command": "${PWD}/adssh-mcp",
      "env": {
        "ADSSH_MCP_API_KEY": "${ADSSH_MCP_API_KEY}",
        "ADSSH_POLICY": "${HOME}/.adssh/policy.rego"
      }
    }
  }
}
```

Notes for the executor:
- The `command` path should use the project-root-relative binary path. The adssh-mcp binary is built to the project root via `go build -o adssh-mcp ./cmd/adssh-mcp`. Use an absolute path — check where the binary currently exists (likely `./adssh-mcp` in project root) and use the full absolute path.
- `ADSSH_MCP_API_KEY` uses env var interpolation so the actual key is never stored in the file.
- `ADSSH_POLICY` points to the default policy location.
- This is the env-var form per D-05. The CLI-flag alternative is documented in the reference doc, not needed in this config file.
  </action>
  <verify>
    <automated>test -f .mcp.json && echo "EXISTS" || echo "MISSING"</automated>
    <automated>python3 -c "import json; json.load(open('.mcp.json')); print('VALID JSON')" 2>&1</automated>
    <automated>grep -c "mcpServers" .mcp.json</automated>
    <automated>grep -c "adssh-mcp" .mcp.json</automated>
  </verify>
  <acceptance_criteria>
    - .mcp.json exists at project root
    - File is valid JSON (python3 json.load succeeds)
    - File contains `"mcpServers"` key
    - File contains `"adssh"` server entry
    - File contains `"command"` pointing to adssh-mcp binary
    - File contains `"ADSSH_MCP_API_KEY"` in env block
    - File contains `"ADSSH_POLICY"` in env block
    - File does NOT contain `"args"` key (env-var form, not CLI-flag form)
  </acceptance_criteria>
  <done>
    .mcp.json exists at project root with valid JSON, registering the adssh MCP server using env-var configuration (per D-05). Contains mcpServers.adssh entry with command path to adssh-mcp binary and env vars for ADSSH_MCP_API_KEY and ADSSH_POLICY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .mcp.json -> binary spawn | Claude Code reads .mcp.json and spawns adssh-mcp as subprocess |
| SKILL.md -> Claude context | Skill body loaded into Claude's context on trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Spoofing | .mcp.json command path | mitigate | Use absolute path to adssh-mcp binary. User must verify the path points to the correct binary. Git-tracked file provides integrity check. |
| T-03-04 | Information Disclosure | .mcp.json env vars | mitigate | ADSSH_MCP_API_KEY uses `${ADSSH_MCP_API_KEY}` env var interpolation — actual secret is NOT stored in the file. File should NOT be committed with real secrets. |
| T-03-05 | Tampering | SKILL.md | accept | Local file with same trust level as project code. Git-tracked for integrity. |
</threat_model>

<verification>
- `.claude/skills/adssh/SKILL.md` exists with correct frontmatter (name, triggers, no allowed-tools)
- SKILL.md body includes connect + orient sequence referencing list_sessions
- SKILL.md body includes `@.claude/adssh-reference.md` reference
- `.mcp.json` exists, is valid JSON, and contains adssh server registration
- `.mcp.json` uses env-var form (no args array)
</verification>

<success_criteria>
SKILL.md triggers on `/adssh`, instructs Claude to connect + orient (D-03), and loads the companion reference (D-01). .mcp.json registers the adssh-mcp binary with env-var configuration (D-05).
</success_criteria>

<output>
After completion, create `.planning/phases/03-claude-code-skill/03-02-SUMMARY.md`
</output>
